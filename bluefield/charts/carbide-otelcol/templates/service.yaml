apiVersion: v1
kind: Service
metadata:
  name: {{ include "carbide-otelcol.fullname" . }}
  labels:
    {{- include "carbide-otelcol.labels" . | nindent 4 }}
spec:
  type: ClusterIP
  internalTrafficPolicy: Local
  selector:
    {{- include "carbide-otelcol.selectorLabels" . | nindent 4 }}
    {{- with .Values.serviceDaemonSet.labels }}
      {{- toYaml . | nindent 4 }}
    {{- end }}
  ports:
    - name: prometheus
      protocol: TCP
      port: {{ include "carbide-otelcol.prometheusPort" . | int }}
      targetPort: {{ include "carbide-otelcol.prometheusPort" . | int }}
